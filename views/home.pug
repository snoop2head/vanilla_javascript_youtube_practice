extends layouts/main
include mixins/videoBlock

block content
    .videos
        //- getting item from video database
        each item in videos
            //- inputting data to videoBlock mixin in order to yield html
            +videoBlock({
                title:item.title,
                views:item.views,
                videoURL:item.videoURL
            }) 
