//- mixin function that yields html code
mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoURL, controls=true)
        h4.videoBlock__title=video.title
        h6.videoBlock__views=video.views 
